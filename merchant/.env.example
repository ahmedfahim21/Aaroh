# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/postgres
POSTGRES_URL=****

# Privy wallet connect — required for merchant auth
# Get your app ID at https://dashboard.privy.io
NEXT_PUBLIC_PRIVY_APP_ID=

# URL of the consumer app (for cross-app redirects)
NEXT_PUBLIC_CONSUMER_APP_URL=http://localhost:3000
